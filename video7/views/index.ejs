<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900 text-white">
      <div class="form p-10">
        <form action="/create" method="post">
          <input
            class="block px-5 py-2 bg-zinc-800 w-full rounded-lg outline-none"
            type="text"
            placeholder="Title goes here"
            name="title"
          />
          <textarea
            class="block px-5 py-2 resize-none mt-2 bg-zinc-800 w-full rounded-lg outline-none"
            placeholder="Write your task details"
            name="details"
          ></textarea>
          <input
            class="block mt-2 bg-cyan-600 px-5 py-2"
            type="submit"
            value="create task"
          />
        </form>
      </div>
      <div class="tasks  p-10 flex gap-3 flex-wrap">
        <% if (files.length > 0) { %> <% files.forEach(function(val){ %>
        <div class="task min-w-72 px-3 py-4 rounded-lg bg-zinc-800">
          <h1 class="text-white text-3xl tracking-tighter">
            <%= val.replace(".txt", "") %>
          </h1>
          <a class="text-blue-600 inline-block mt-2" href="#">Read More</a>
        </div>
        <% }) %> <% } else { %>
        <h3 class="text-white">No task yet</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
